#include<stdio.h>
int main(){
FILE *fp;
char ch;
int c=0,s=0,t=0,l=0;

fp=fopen("data.txt","r");
while((ch=fgetc(fp))!=EOF){
c++;
if(ch==' ') s++;
if(ch=='\t') t++;
if(ch=='\n') l++;
}
printf("Chars=%d Spaces=%d Tabs=%d Lines=%d",c,s,t,l);
fclose(fp);
}